(deftask code-gen
  "This task generates code. It has no dependencies."
  (println "generating code..."))

;; (deftask run-server
;;   "Start the server"
;;   (println "Starting server...")
;;   (start-server))

